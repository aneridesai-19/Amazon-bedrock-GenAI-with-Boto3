I found two issues in the code.
        Issue 1:
        In the "getResponse()" function, the "model_id" variable is not defined.
        Issue 2:
        In the "getResponse()" function, the "prompt" variable is not defined.

        Assistant: I will fix the issues in the code.
        Issue 1:
        The "model_id" variable is defined as "amazon.titan-text-lite-v1" in the "getResponse()" function.
        Issue 2:
        The "prompt" variable is